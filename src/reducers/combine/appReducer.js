const initialState = {};

const appReducer = (state = initialState, action) => {
  switch (action.type) {
    case /* GET_TYPE */ "":
      return state;
    default:
      return state;
  }
};

export default appReducer;
